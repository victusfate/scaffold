# Enforce the Node floor declared in package.json "engines" (>=23.6.0).
# Scaffold's source is TypeScript run via Node's native type-stripping, which is
# default-on from Node 23.6 — on an older Node the .ts entry points fail to parse.
# engine-strict makes `npm install` hard-fail with a clear message instead.
engine-strict=true
