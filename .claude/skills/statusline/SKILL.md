---
description: Turn the usage statusLine on or off (model + context% + 5-hour usage% in every project) without editing config by hand. Runs bin/install-statusline.sh, which installs the script to ~/.claude and merges the setting without clobbering other keys. Opt-in, global, run once per machine; `/statusline off` disables it.
---

@../../../skills/statusline.md
