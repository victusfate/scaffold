---
name: diagram
description: |
  Generate or update a mermaid diagram with the `.mmd` text as source of truth — render locally to SVG by default, live-preview side by side in VS Code, and publish a mermaid.live URL only on request. Use for architecture, flowchart, sequence, ER, state, or class diagrams; "diagram this", "draw the architecture", "make a mermaid diagram", "render this mermaid", or "/diagram". Never converts to a lossy shape format.
license: MIT
metadata:
  version: "1.0"
---

Read and follow the complete skill instructions in [`skills/diagram.md`](../../../skills/diagram.md).
